plugins { id("com.android.application"); id("org.jetbrains.kotlin.android") }
android {
    namespace = "id.sch.ashidiq.cbt"
compileSdk = 36
    defaultConfig {
      applicationId = "com.ashidiq.test"
      minSdk = 26
      targetSdk = 36
        versionCode = 8
        versionName = "1.7"
        buildConfigField("String", "API_URL", "\"https://cbt.smpmuashidiq.sch.id/backend/api.php\"")
        buildConfigField("String", "WEB_URL", "\"https://cbt.smpmuashidiq.sch.id/index.html\"")
        // "webview" = HYBRID (dipakai sekarang): halaman ujian di WebView dalam app supaya lockdown penuh
        //         (FLAG_SECURE, kunci JS, re-entry code) tetap jalan, tapi Google Form-nya dititipkan ke
        //         Chrome lewat tombol "BUKA SOAL DI CHROME" agar memakai profil & akun Google HP.
        // "twa"   = seluruh halaman dijalankan Chrome (Trusted Web Activity). Ikut profil Chrome, tapi
        //         kunci JS dan FLAG_SECURE hilang.
        buildConfigField("String", "LAUNCH_MODE", "\"webview\"")
    }
    signingConfigs {
        release {
            storeFile file("../../ashidiq-release.jks")
            storePassword "AshidiqCBT2026!"
            keyAlias "ashidiq"
            keyPassword "AshidiqCBT2026!"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled = true
            shrinkResources = true
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
    compileOptions { sourceCompatibility = JavaVersion.VERSION_17; targetCompatibility = JavaVersion.VERSION_17 }
    kotlinOptions { jvmTarget = "17" }
    buildFeatures { buildConfig = true }
}
dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.browser:browser:1.8.0")
}
